INSTRUCTIONS:

Burn the iso to a cd or mount it to a virtual drive. during setup it asks for activation key. run keygen.exe to get an activation key. (you must click activate by phone)

CONTENTS:

photoshopcs2,iso
keygen.exe