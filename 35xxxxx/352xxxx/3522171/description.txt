Tom Tom navigator 5 for the pocket pc.
The setup file and directory structure is packed into this rar file.
When unpacked just run the (cracked) setup file, it is the only setup file in the root directory.

Then you'll have the dutch language TomTom 5 for the pocket pc, 
with flashing locations.