Contains 3 directories and one .nfo file.  The first 2 directories each contain a rar file that extracts to an avi file of suitable size to write to a CD.  There is also a directory containing a sample clip.