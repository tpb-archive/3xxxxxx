 http://lt.k1011.nutime.de/

Linux Mint is an Ubuntu-derived (GNOME desktop) distribution. Unlike Ubuntu it is NOT strictly free/open-source, so it installs default with Flash, codecs, etc. (not 3d drivers though) It retains the ease-of-use of Ubuntu.